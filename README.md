# CombiNBT
This comand line program is designed to help when you are using the vanila tweaks track raw statistics datapack and a player changes their username causing a whole new set of statistics to be generated.

to run the binary use `./CombiNBT "old player name" "new player name"`